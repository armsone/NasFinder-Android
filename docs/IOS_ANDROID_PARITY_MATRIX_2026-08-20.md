# NasFinder iOS → Android parity matrix (2026-08-20)

## Evidence profile

- Scope: user-visible iOS changes in `65f55a0`, `eecda73`, `75d8f17`, and `1dfafa3`, checked against current Android source and the apparent counterpart commits `cc0b583`, `b62d417`, `741a1f6`, and `310cc7d`.
- Reference: iOS `1dfafa3`; Android baseline `310cc7d` plus the local changes recorded below.
- Source profile: Korean locale, system theme plus theme-specific strings, phone and tablet-supported code paths, portrait/landscape where the same rendering path is used.
- Runtime profile: all Android JVM unit tests and the `releaseQa` build passed. The exact verified v6 APK was installed with data preservation on an SM-F968N; the dashboard storage entry opened Android DocumentsUI and the Inbox exposed its import action. No file was selected, so copy/persistence and partial-failure fixtures remain `확인 필요`.
- Original lossless paired captures/SHA-256: no matching iOS capture was available. Android verification captures were inspected locally but not retained because the dashboard contained personal connection data. Pixel geometry, animations, gesture feel, and tablet rendering remain `확인 필요`.

## Atomic matrix

| Route/state ID | Element/anatomy | Dimension/action | Fixture/profile | iOS exact reference | Android observed | Difference | Required action | Evidence/confidence | Status/exception proof |
|---|---|---|---|---|---|---|---|---|---|
| `dashboard_storage_default` | Storage row, leading folder artwork | Presence and action | ko-KR, any theme, phone/tablet shared path | `ConnectionListView.swift:280-313`: tappable folder row opens a multiple-selection file importer | Baseline `DeviceStorageCard` was non-clickable; local `NasFinderApp.kt:643-646,791-792,930-953` launches `OpenMultipleDocuments` | Functional entry point was missing | Add picker launcher and tappable storage row | Source + unit/build + SM-F968N, High | DocumentsUI launch verified; paired visual/tablet `확인 필요` |
| `dashboard_storage_default` | Primary/supporting labels | Exact platform-owned wording | ko-KR | `iPhone 저장공간`; `파일 앱 열기 · iCloud Drive 지원` | `Android 저장공간`; `파일 선택기 열기 · 클라우드 저장소 지원` | Platform file-provider names differ | Preserve equivalent OS-owned provider wording while retaining hierarchy | Source, High | Forced OS wording exception; app-owned geometry `확인 필요` |
| `dashboard_storage_default` | Capacity indicator | Presence and ordering | Storage capacity available | `ConnectionListView.swift:315-323`: label, progress, percentage | `NasFinderApp.kt:955-961`: label, progress, percentage | Baseline already had capacity; moved below import row | Keep indicator after the picker row | Source + compile, High | Implemented from source; visual `확인 필요` |
| `inbox_default` | Top-bar import action | Icon, exact label, visibility | Empty and populated; not selecting | `ReceivedFilesView.swift:71-77`: `파일에서 가져오기`, `doc.badge.plus` | Local `NasFinderApp.kt:2096-2098,2128-2133`: `파일에서 가져오기`, auto-mirrored note-plus glyph | Action was missing | Add action outside selection mode | Source + unit/build + SM-F968N, High | Empty-state action and accessibility label verified; populated/paired visual `확인 필요` |
| `inbox_import_success` | File picker result | Multi-select, copy, navigation, persistence | One or more readable content URIs | `ConnectionListView.swift:106-118`; `ReceivedFilesView.swift:90-96`; `SharedInboxStore.importFromFiles` | Local `NasFinderViewModel.kt:2250-2285` copies each distinct URI into `SharedInboxStore`, refreshes records, opens Inbox | Entire in-app import flow was missing | Add picker import pipeline using existing durable inbox store | Source + compile + existing store tests, High | Implemented from source; runtime picker/persistence `확인 필요` |
| `inbox_import_partial_failure` | Error state | Partial/all-failure result | Mixed readable/unreadable URIs | iOS reports count of failed files after retaining successful imports | Android reports all-failed or partial-failure counts and retains successful imports | Baseline had no state | Add per-item isolation and error summary | Source + compile, Medium | Implemented from source; runtime error fixture `확인 필요` |
| `connection_delete_confirm` | Supporting text | Platform device name | Saved connection, delete requested | `ConnectionListView.swift:145-157`: `이 iPhone에서 제거됩니다` | Local `NasFinderApp.kt:800-804`: `이 Android 기기에서 제거됩니다` | Baseline incorrectly said iPhone | Replace only platform-owned noun | Source + contract test, High | Forced OS wording exception; matched behavior |
| `settings_theme_system` | Theme description | Platform device name | System theme selected | `AppSettingsView.swift:95-98`: `iPhone의 라이트·다크 모드...` | Local `NasFinderApp.kt:3374`: `Android의 라이트·다크 모드...` | Baseline incorrectly said iPhone | Replace platform-owned noun | Source + contract test, High | Forced OS wording exception |
| `settings_theme_grid` | System theme supporting label | Platform settings name | System theme tile | `SkyBreezeTheme.swift:27-35`: `iPhone 설정` | Local `NasFinderApp.kt:3584`: `Android 설정` | Baseline incorrectly said iPhone | Replace platform-owned noun | Source + contract test, High | Forced OS wording exception |
| `settings_app_icon` | Five icon choices | Artwork, labels, selection/persistence | All themes, each choice | `AppIconSettingsView.swift`: blue, purple, Vibe Coder, Cyber Vault, Network NAS | Android has all five variants, aliases, labels, progress, selected state, and persistence | No source-level omission found | No code change | Source/tests, High | Source aligned; paired launcher/UI captures `확인 필요` |
| `dashboard_recent_work` | Logo/header/recent interactions | Content and action | Empty/populated | iOS `eecda73` | Android `b62d417` and current `NasFinderApp.kt` include the PhoneHard asset and corresponding interactions | No source-level omission found | No code change | Commit/source comparison, High | Source aligned; visual `확인 필요` |
| `browser_history` | Back/close affordance | State transition and hardware back | WebView with/without history | `WebBrowserView.swift`: back when history exists, otherwise close; native edge swipe enabled | `WebBrowserScreen.kt`: same button and Android back behavior; no app-owned iOS-style edge recognizer | Core behavior matches; gesture feel differs by OS navigation | Verify on paired devices before any custom gesture | Source, Medium | Core source aligned; gesture `확인 필요` |
| `super_thumbnail_progress` | Progress presentation | Modal/push animation and escape behavior | Waiting/running/completed | iOS uses a full-screen cover and prevents unsafe exit | Android pushes a progress screen and blocks back while active | Functional guard matches; presentation style may differ | Capture paired states before changing presentation | Source, Medium | Functional source aligned; visual transition `확인 필요` |
| `dashboard_large_width` | Adaptive layout | Phone/tablet arrangement | 600dp+/regular width | Current active iOS dashboard path is a single list; unused grid helpers exist | Current Android path is a single `LazyColumn` | Relative current behavior matches, but both need separate large-screen product work | Do not expand this parity fix beyond the active reference path | Source, Medium | Source aligned for current reference; tablet visual `확인 필요` |

## Verification commands

```sh
./gradlew testDebugUnitTest assembleReleaseQa
NASFINDER_EXPECTED_CERT_SHA256=<established digest> scripts/verify-release-apk.sh /absolute/path/NasFinder-Android-v6.apk
adb install -r /absolute/path/NasFinder-Android-v6.apk
```

There is no complete deterministic paired screenshot catalog in either repository for these states. Until paired post-change captures and behavioral traces exist, this document intentionally does not claim pixel-complete or full runtime parity.

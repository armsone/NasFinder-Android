<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Deliberately expose only prepared shares, verified updates and durable inbox payloads. -->
    <cache-path
        name="prepared_shares"
        path="shares/" />
    <cache-path
        name="verified_updates"
        path="updates/" />
    <files-path
        name="shared_inbox"
        path="SharedInbox/" />
</paths>
